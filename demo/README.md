# Instructions for running the demo

1. Install [VHS](https://github.com/charmbracelet/vhs)

2. Copy this dir to ~/demo

3. Replace `swiftpick.json` contents with `./cwd/swiftpick.json`

4. `cd ~/demo/cwd`

5. Run `vhs demo.tape`
